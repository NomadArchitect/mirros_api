de:
  rule:
    errors:
      messages:
        no_default_board: 'Default-Board kann keine Regeln enthalten'
  board:
    errors:
      messages:
        default_board_deletion: 'Das Default-Board kann nicht gelöscht werden'
        cannot_delete_active_board: 'Das gerade aktive Board kann nicht gelöscht werden'
