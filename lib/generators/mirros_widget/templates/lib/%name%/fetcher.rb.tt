module <%= name.camelcase %>

  class Fetcher
    def initialize(url)
      # TODO
    end

    # TODO: create custom methods here

    private

    def generate_uid(url, index)
      "#{Digest::SHA1.hexdigest(url)}_#{index}"
    end

  end
end
