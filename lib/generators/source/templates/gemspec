require 'json'

$:.push File.expand_path("../lib", __FILE__)

# Maintain your gem's version:
require "<%= name.underscore %>/version"

# Describe your gem and declare its dependencies:
Gem::Specification.new do |s|
  s.name        = "<%= name.underscore %>"
  s.version     = <%= name.camelcase %>::VERSION
  s.authors     = ["<%= @author_name %>"]
  s.email       = ["<%= @author_email %>"]
  s.homepage    = "https://glancr.de/modules/<%= name.underscore %>"
  s.summary     = "Summary of <%= name.camelcase %>."
  s.description = "Description of <%= name.camelcase %>."
  s.license     = "MIT"
  s.metadata    = { 'json' =>
                {
                  type: 'sources',
                  title: {
                    enGb: 'Calendar' # Add your source's title in other languages
                  },
                  description: {
                    enGb: s.description
                    # Add description for other languages keyed by the snakeCased language tag.
                  },
                  groups: [] # see https://gitlab.com/glancr/mirros_one/wikis/home for available groups
                }.to_json
              }

  s.files = Dir["{app,config,db,lib}/**/*", "MIT-LICENSE", "Rakefile", "README.md"]
end
