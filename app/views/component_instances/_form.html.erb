<%= simple_form_for(@component_instance) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :component %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
