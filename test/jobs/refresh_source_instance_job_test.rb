require 'test_helper'

class RefreshSourceInstanceJobTest < ActiveJob::TestCase
  test 'it uses the correct refresh interval' do
    flunk 'not yet implemented, need to mock a base class'
    instance = source_instances :ical
  end
end
